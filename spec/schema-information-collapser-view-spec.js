'use babel';

import SchemaInformationCollapserView from '../lib/schema-information-collapser-view';

describe('SchemaInformationCollapserView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
